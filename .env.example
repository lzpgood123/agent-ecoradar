# Optional: only needed if mcporter Exa is unavailable and you implement direct Exa HTTP fallback.
EXA_API_KEY=

# GitHub CLI should already be authenticated via `gh auth status`.
# Do not commit real credentials.
