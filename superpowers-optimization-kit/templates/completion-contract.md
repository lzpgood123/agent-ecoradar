<!--
完成契约规则（强制）：
1. 验收场景：可新增隐含缺口场景；禁止删除或降级已冻结场景；新增行标记 冻结=后增
2. PRD 目标不可改（不可删除、不可改写语义、不可降级）
3. 宣布完成仅当下方「硬门槛」四项全部勾选为完成（全绿）
4. 已试方法：每次失败后追加；禁止重复相同方法摘要
-->

# 完成契约：{任务名称}

## 状态

- 当前阶段：
- 可否宣布完成：否
- 最后更新：
- 关联 Goal 文档：
- 关联 PRD：

## PRD 目标清单

| # | 目标 | 来源 | 覆盖状态 | 证据路径 |
|---|------|------|---------|---------|
| 1 | {目标} | PRD | pending | |

> 覆盖状态：`pending` | `in_progress` | `done`。`done` 时必须有可核验证据路径。

## 验收场景

| # | 场景 | 类型 | 证据要求 | 状态 | 证据路径 | 冻结 |
|---|------|------|---------|------|---------|------|
| 1 | {场景} | 功能 | {实测要求} | pending | | 是 |

> 状态：`pending` | `in_progress` | `done` | `blocked`。
> 冻结：`是`（Intake 冻结）| `后增`（运行中新增）| `否`（未冻结，Intake 前可调）。

## 决策矩阵

| # | 决策点 | 决策 | 来源 | 置信度 |
|---|--------|------|------|--------|
| 1 | {决策点} | {决策} | PRD \| P1 \| wiki \| inferred | high \| medium \| low |

## 未决项

| # | 问题 | 状态 | 处理方式 |
|---|------|------|---------|
| — | 无 | closed | — |

> 状态：`open` | `resolved` | `closed`。存在 `open` 冲突项时不可宣布完成。

## 已试方法（禁止原地踏步）

| # | 失败门禁 | 方法摘要 | 结果 | 时间 |
|---|---------|---------|------|------|
| — | — | — | — | — |

## 硬门槛

- [ ] PRD 目标全部覆盖
- [ ] 验收场景全部有实测证据
- [ ] 无未决冲突
- [ ] Wiki/检查点合规

> 仅当以上四项全部勾选，才可将「可否宣布完成」改为「是」。
